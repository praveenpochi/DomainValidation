package com.validation;

public interface ValidationGroups  {

	public interface ConditionalValidation {
		
	}
	interface ValidateCA {
        // validation group marker interface
    }

    interface ValidateCHK {
        // validation group marker interface
    }
    
    interface ValidateACH {
        // validation group marker interface
    }
}